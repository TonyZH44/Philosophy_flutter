import 'package:flutter/material.dart';

class SeminarPage extends StatelessWidget {
  const SeminarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SeminarPage'),
      ),
    );
  }
}